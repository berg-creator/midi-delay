# Сессия 24: красная CI — стресс анти-клика на Windows и плавающий Tempo Sync

## Задачи этой сессии
- #25 Анти-клик: стресс на MSVC выдал отношение 9,7 при пороге 8. Щелчок или разброс
- #54 pluginval: `Tempo Sync` плавает и теперь роняет CI на macOS — решить до релиза
- Если осталось место — #34: регрессия на Windows, до которой тест пока не доходит

## Что случилось в сессии 23

Рендеров нет: звук не менялся. Вердикт по BERG-S22 — «пара», записан в `## [55]`.
#42 закрыт — файл лицензий в обоих бандлах. Имя — Berg, домен не куплен (`## [49]`).
Членство Apple — «не сейчас», первый macOS-релиз выходит pkg без подписи (RELEASE §4).
**Push сделан, CI впервые с сессии 05 — и она красная на обеих платформах:**

| Платформа | Где падает | Разбор |
|---|---|---|
| Windows | ProcessorTest, «Анти-клик» 48 кГц 300 с: худший шаг 0,8939, 99,9 % 0,0923, отношение 9,7 | `## [25]`, «Сессия 23» |
| macOS | pluginval, `Tempo Sync not restored`, 0,711 вместо 1 — во втором прогоне из двух | `## [54]`, «Сессия 23» |

На macOS тот же стресс даёт 4,9, локально и в CI одинаково. До регрессионных рендеров
Windows не доходит, файл лицензий в Windows-бандле не проверен (`## [34]`, «Второй прогон»).

## Что прочитать перед началом (и ничего сверх этого)
- `docs/ISSUES.md` — `## [25]` целиком, `## [54]` целиком, в `## [34]` абзацы «CI, сессия 23»
- `Source/test_processor.cpp` — раздел «Анти-клик» (стресс около строки 3800) и комментарий
  со списком мест разрыва перед ним
- `.github/workflows/` — шаги ProcessorTest и pluginval

## Ответы, которых ждёт эта сессия

Если ответы в стартовом сообщении пустые — **окном вопросов в первую минуту**.

1. **Домен `berg.audio`: покупаем / не сейчас / другой: <...>.** Свободен на 13.09.2026,
   премиальная цена не проверена. Код от ответа не зависит — это запись в `## [49]`.
2. **Есть ли доступ к Windows с DAW (своя машина, знакомый): да / нет.** «Да» — сессия,
   найдя подозрительное место, готовит сборку и сценарий, чтобы щелчок послушали ухом.

## Что сделать
1. **#25, сначала замер, потом вывод.** В стрессе запоминать id последнего рывка параметра,
   его значение и номер блока и печатать их для худшего шага. Прогнать локально на arm64
   и под Rosetta (x86_64: отдельный каталог сборки с `-DCMAKE_OSX_ARCHITECTURES=x86_64`) —
   это ближайшее к MSVC. Push — и смотреть, что печатает Windows.
2. По тому, что напечатано: **настоящий разрыв** (одно место, шаг, которого на сглаженном
   параметре быть не должно) — чинить тем же приёмом, что в `## [25]` (сглаженный множитель),
   и лечение подтверждать на той же траектории. **Разброс материала** (худший шаг на рывке,
   который и так объявлен безопасным, или на резком входе) — порог не двигать молча:
   записать замер и довод в `## [25]` и только потом менять проверку.
3. **#54.** Либо лечить `Tempo Sync`, либо в CI пропускать ровно эту проверку
   (`--disabled-tests` у pluginval, если есть в v1.0.4 — проверить `--help`) с записью
   в `## [54]`. Плавающая красная CI не остаётся.
4. Вывод CI после каждого push записывать в `## [34]` / `## [31]` — номер прогона и числа.

## Готово, когда
- [ ] Для худшего шага стресса на Windows известен параметр и блок; вывод записан в `## [25]`
- [ ] Стресс зелёный на обеих платформах — починкой или записанным доводом, но не молча
- [ ] #54 решён: вылечен или пропущен в CI с записью; CI на macOS зелёная два прогона подряд
- [ ] Если Windows дошла до регрессии — запасы до допуска записаны в `## [34]`
- [ ] Эталоны регрессии не переписаны без замера; `git status tests/regression` пуст,
      если звук не менялся
- [ ] Если звук менялся — рендеры в `~/Downloads/BERG-S24/` и таблица в следующем `NEXT.md`
- [ ] pluginval — только с `--skip-gui-tests` (бинарник: `/private/tmp/pluginval.zip`); `auval` — предупредив
- [ ] Тексты в окне — только ASCII по-английски
- [ ] Изменения закоммичены, сообщение на русском; push разрешён пользователем в сессии 23
- [ ] `prompts/PROGRESS.md` обновлён
- [ ] Этот файл перемещён в `prompts/archive/S24-ci-red.md`
- [ ] Написан новый `prompts/NEXT.md` для сессии 25
- [ ] Стартовое сообщение для сессии 25 — с вопросами на одну строку

## Что не брать
- **Сайт, подписка и FAQ из #49** — сессия 25, если CI зелёная.
- **RELEASE §5 и #33** — членство не куплено.
- **#57 Ghost** — 1.1. **#50 рассылка** — после подписанных сборок и сайта.
- **#52 оформление, #53 Arp Echo** — не в релизной ветке.

## Что уже решено, переспрашивать не надо
- **Ghost Choir — пара** (вердикт сессии 23, `## [55]`). **Хор — пара, ping-pong — по нотам** (S22).
- **Релиз с нынешним звуком, Ghost в 1.1** (S22, `## [57]`).
- **Имя — Berg**, не «Berg Audio» (S23, `## [49]`). Идентификаторы не трогать.
- **Первый macOS-релиз — pkg без подписи** (S23, RELEASE §4). zip без подписи не выпускается.
- **JUCE — Starter EULA, в поставке ничего не требует** (S23, `## [42]`).
- **Push разрешён** (S23).

## Известные риски

1. **Вердикт уха отменяет замер.** Если найденный разрыв сомнительный — рендер и вопрос,
   а не порог.
2. **Промпт предыдущей сессии может быть неправ** — восемь случаев (в S23: #42 ссылался
   на `## [3]` за условиями JUCE, а там CI). Расходиться можно, записав почему.
3. **Каждый push отменяет недосчитанный прогон** (`concurrency` в workflow). Пушить,
   когда предыдущий досчитан или не нужен. Прогон — около 10 минут.
4. **Rosetta — не MSVC.** Если под x86_64 стресс зелёный, это не опровергает Windows.
5. **Окна поверх работы пользователя** — `auval`, Installer.app — только с предупреждением.
6. **Переименование `mke` → `Mike`** — `rm -rf build` и заново.

## Следующая сессия займётся
Если CI зелёная — сайт и FAQ из #49. Если нет — продолжать эту.
