# Сессия 16: интерфейс — LookAndFeel и визуализация

## Задачи этой сессии
- #26 Каркас редактора и LookAndFeel — осталась вторая половина: собственно дизайн
- #27 Визуализация активных нот и хвостов

M3 закрыта целиком в сессии 15. M4 наполовину: пресеты стоят, интерфейс — нет.

## Что случилось в сессии 15 (важнее списка задач)

**#48 закрыта, #44 закрыта, веха M3 закрыта целиком.** Шесть фабричных пресетов
заведены через штатный интерфейс программ JUCE, то есть видны и в меню хоста,
и в списке в шапке окна. Насыщение встало на отвод обратной связи без ручки Drive.

**Пользователь вынес вердикт по рендерам сессии 14: «сетка работает, оба варианта
читки чёткие».** Это сняло претензию «поёт не в такт» (виновата была не сетка, а её
отсутствие) и подтвердило разрез пресетов по материалу.

**Вердикт по шести рендерам получен ещё в сессии 15 — переспрашивать не надо.**
Пять пресетов из шести названы рабочими, «о, что это» получено. Порядок и дословные
формулировки — `docs/ISSUES.md` `## [48]`, раздел «Вердикт по шести рендерам сессии 15».
Оттуда же вышли две правки, обе уже сделаны:

- **Arp Echo переведён на Mix 30.** Единственная претензия была к нему: «дилэй слишком
  громкий, обычно его делают еле заметным». Правило шире одного пресета и записано
  в `STRATEGY.md` §5: **громко — то, что добавляет голос; тихо — то, что повторяет.**
- **Компенсация выхода не нужна, вопрос закрыт.** Замер показывал, что громкий пресет
  делает трек тише на 6 dB; ухо ответило «слишком громко». Закон микса не трогать,
  `outputGain` в пресеты не добавлять.

**Что осталось спросить — только одно число.** Width 150 с ping-pong в пресете
Sung Vocal теряет в моно 1,61 dB (таблица в `## [23]`). Значение одобрено ухом
в сессии 13, замер сделан в сессии 15, вердикта по нему не было. Замена — одна строка.

**Один рендер переслушать: `~/Downloads/BERG-S15/s15-2-arp-echo.wav`** — он перерендерен
после правки Mix 30, прежний вердикт относится к старой версии.

**Два замечания из вердикта оказались не про пресеты, и это ловушка на будущее.**
«В начале не было дилэя» — это пять секунд обхода в начале каждого рендера, сделанные
нарочно. «Долгая атака» у Dense Rap — не атака (там 10 мс), а целый такт до повтора
на материале, под который пресет не рассчитан. **Всё, что в рендере сделано ради
демонстрации, называть в таблице рендеров явно**, иначе оно вернётся вердиктом о звуке.

## Что прочитать перед началом (и ничего сверх этого)
- `docs/ISSUES.md` — разделы `## [26]`, `## [27]`, `## [29]` целиком; `## [48]`
  только раздел «Открытое: громкий пресет делает трек тише»
- `docs/ROADMAP.md` — врезка про M4 («M4 подорожала»): почему пятнадцать ручек
  в одинаковой сетке уже не объясняют себя
- `Source/PluginEditor.cpp` и `PluginEditor.h` целиком — файл маленький, 240 строк
- `Source/PluginProcessor.h` — публичная часть: что редактор уже может спросить
  у процессора (`getUnisonNote`, `isFollowMode`, `isSyncMode`, `getSyncBpm`,
  `requestedDelayMs`, `getMinDelayMs`, `getAlignmentMs`, `midiNoteCount`, `lastNote`)

## Что сделать

### 0. Забрать два ответа — до кода

Вердикт по пресетам уже есть, спрашивать надо только два: переслушанный Arp Echo
и Width 150 в Sung Vocal. Оба — одной строкой, и оба не блокируют интерфейс:
если ответа нет, делать #27 и #26, а эти две строки поправить в конце.

### 1. #27 визуализация — раньше #26

Порядок обратный номерам, и это намеренно. #27 решает названную пользователем проблему
(«плагин не работает» — а на деле не доехал MIDI), #26 решает вопрос вкуса. Если сессия
кончится на середине, полезнее иметь работающий индикатор в старом оформлении,
чем красивое окно без индикатора.

Что показывать: приходит ли MIDI, какие ноты звучат сейчас, сколько голосов занято,
где унисон. Три первых пункта частично есть в шапке текстом — их надо превратить
в то, что читается не читая. Обмен с аудиопотоком **только atomic или FIFO**,
локов в `processBlock` нет и не будет.

### 2. #26 LookAndFeel и группировка по сигнальному пути

Сетка 4×4 из одинаковых ручек — это список, а не интерфейс. Группы названы в задаче:
Delay, Pitch, Envelope, Mix. У трёх параметров смысл зависит от режима (Delay Time
в Follow, Width при ping-pong, division при выключенном Sync) — это и есть главная
причина, по которой одинаковые ручки врут.

Список пресетов уже стоит в шапке справа (`presetBox` в `PluginEditor.cpp`) —
его надо вписать в новое оформление, а не заводить заново.

### 3. Что не брать
- **#29 ресайз и DPI.** После LookAndFeel, иначе переделывать дважды.
- **#28 пользовательские пресеты.** Фабричные закрыты, пользовательские — это файлы
  и диалоги, отдельная работа и не про внешний вид.
- **#51 Prelude (наплыв из будущего).** Идея пользователя, разбор в `## [51]`.
  Она про DSP, а не про интерфейс, и в эту сессию не помещается.

## Готово, когда
- [ ] Ответ по перерендеренному Arp Echo (Mix 30) получен и записан в `## [48]`
- [ ] Ответ по Width 150 в Sung Vocal получен и записан в `## [23]`
- [ ] #27: видно, что MIDI приходит, какие ноты активны и сколько голосов занято;
      при отсутствии MIDI видна явная подсказка; обмен без локов
- [ ] #26: визуальная группировка отражает сигнальный путь; свой LookAndFeel
- [ ] Редактор открывается и закрывается многократно без утечек (это ловит pluginval)
- [ ] Тексты в окне — только ASCII по-английски (CLAUDE.md; проверено в FL, кириллица
      рассыпается). В тесте пресетов уже стоит такая проверка, брать пример оттуда
- [ ] `cmake --build build --target ProcessorTest && ./build/ProcessorTest_artefacts/Release/ProcessorTest` — зелёный
- [ ] Офлайн-тесты буфера и питчера собираются одной командой clang и зелёные
      (команды — в шапке `Source/DSP/test_delay_buffer.cpp` и `test_pitch_shifter.cpp`)
- [ ] `ProcessorTest --bench hq hold` — цифра не выросла заметно против 4,4 % ядра
- [ ] pluginval strictness 10 зелёный (тесты Editor и Editor Automation — главные здесь),
      `auval -v aumf Mdly Berg` зелёный
- [ ] Изменения закоммичены, сообщение на русском
- [ ] `prompts/PROGRESS.md` обновлён
- [ ] Этот файл перемещён в `prompts/archive/S16-editor.md`
- [ ] Написан новый `prompts/NEXT.md` для сессии 17
- [ ] Написано стартовое сообщение для сессии 17 в чат

## Что уже решено, переспрашивать не надо
- **Пресеты — штатный интерфейс программ JUCE, не свой список.** Тогда они видны
  в меню хоста. Разбор в `## [48]`.
- **Пресет — снимок целиком: всё неназванное возвращается к умолчанию.** Кроме Bypass.
- **Ручки Drive у насыщения нет.** Петля на 95 % идёт вниз, а не вразнос: это потолок,
  а не характер, и объяснять ручке нечего. Обоснование в `## [44]`.
- **Sync — галка, а не третий пункт Time Mode** (`## [20]`).
- **Formants по умолчанию Shift, Ducking 50 %** — слуховые решения сессии 13.
- **#35 (детекция питча) остаётся в Post-MVP.**
- **Громко — то, что добавляет голос; тихо — то, что повторяет.** Проверено слухом
  на шести рендерах, записано в `STRATEGY.md` §5. К громкости пресетов не возвращаться.
- **WebView UI не брать** — записано в `## [26]`.

## Известные риски

1. **Тесты меряют движок, пресет — продуктовое решение.** В сессии 15 умолчание
   Chord Pad положило тридцать семь тестов разом. Хелпер `engineDefaults` в
   `test_processor.cpp` возвращает параметры к умолчаниям, и каждый тестовый экземпляр
   его зовёт. Новый тест обязан звать его тоже, иначе он меряет пресет.

2. **Абсолютный порог анти-клика зависит от материала.** Сессия 15 чуть не искала
   несуществующий разрыв: отношение 8,1 при пороге 8 оказалось свойством одного тона,
   а не плагина. Контроль (тот же прогон без изучаемого изменения) дал 7,8.
   **Где порог зависит от материала — критерий должен быть относительным.**

3. **Ninja не пересобирает файл, восстановленный через `mv` из `.bak`.** Время
   модификации приезжает вместе с файлом и оказывается старше объектника. `touch`
   после восстановления.

4. **Метрики в этом проекте регулярно расходятся с ухом, и ухо главнее.** Пятый случай
   подряд. Порядок прежний: сделал — отрендерил — послушал — потом решаешь.

5. **Промпт предыдущей сессии может быть неправ, и это случалось трижды.** Расходиться
   можно, записав почему.

## Следующая сессия займётся
#29 (ресайз и DPI) и подготовкой M5.
