# Сессия 13: вторая половина качественного дилея — модуляция, ducking, моно

## Задачи этой сессии
- #46 Модуляция времени: wow и flutter
- #47 Ducking: хвост приседает под сухим
- #23 Ping-pong и замер моносовместимости (Width и пан по голосам уже сделаны)

## Что прочитать перед началом (и ничего сверх этого)
- `docs/ISSUES.md` — разделы `## [46]`, `## [47]`, `## [23]`, и разделы «Сделано
  в сессии 12» в `## [45]` и `## [22]`: там устройство петли, в которую всё это встанет
- `prompts/PROGRESS.md` — «Находки сессии 12», целиком. Три из шести находок —
  про то, как именно ломается вставка в петлю обратной связи
- `Source/PluginProcessor.cpp` — `renderSegment` целиком, это двадцать строк
- `Source/DSP/Diffuser.h` — образец того, как в этом проекте выглядит DSP-единица
- `Source/test_processor.cpp` — раздел «Диффузия и фильтры в петле (#45, #22)»:
  там готовые `loopResponse`, `centroid` и `plainLoop`, переиспользовать, а не писать заново
- `Source/PluginEditor.cpp` — массив `layout` в начале файла

`PitchShifter.cpp`, `Voice.cpp` и `DelayBuffer.cpp` читать не нужно.

## Что сделать

1. **#46 модуляция времени.** Развилка из описания задачи — модулировать позицию чтения
   петли или позицию чтения голосов — решается **на слух и записывается с обоснованием**.
   Механика есть: кольцо уже читает дробную позицию с интерполяцией Catmull-Rom.
   Ориентир 0,1–3 Гц, глубина 0,1–5 мс.

   **Ловушка ровно та же, что была у диффузии, и она уже подтверждена замером.** Всё,
   что вставляется в петлю, меняет её длину; всё, что вставляется «с обходом на нуле»,
   даёт холодный старт при включении. Рабочая схема из сессии 12: узел крутится всегда,
   а подмешивается множителем — тогда на нуле ручки петля бит-в-бит прежняя, и щелчка
   при включении нет. Не изобретать заново, посмотреть в `renderSegment`.

2. **#47 ducking.** Детектор огибающей на сухом входе, его выход приглушает wet.
   Ориентир: атака 5–20 мс, спад 100–500 мс, глубина до −12 dB.

   Детектор берёт **сухой вход**, а не выход. И берёт его до `dryDelay`, потому что
   в Follow сухой придерживается на латентность питчера: детектор, снятый после линии,
   в Follow опоздает ровно на неё. Критерий «работает одинаково в обоих режимах Time Mode»
   именно про это.

3. **#23 моносовместимость и ping-pong.** Диффузор уже даёт разные длины на левый и правый
   канал — то есть стереокартина в сессии 12 стала шире, а мерили её последний раз до этого.
   Сначала **замер корреляции в моно** на существующей схеме, потом решение про ping-pong.
   Может оказаться, что ping-pong уже избыточен.

4. **Подключил параметр — выведи его в окно.** Массив `layout` в начале
   `Source/PluginEditor.cpp`, сетка на четыре колонки. Сейчас в ней 17 органов.

5. **Не брать #48 (пресеты) в эту сессию.** Пресеты поверх непроверенных модуляции
   и ducking придётся переделывать сразу же.

6. **Проверить в живом хосте.** Правило из `docs/VISION.md` §6, выведено дорогой ценой
   в сессии 11 и подтверждено в сессии 12: офлайн-замер на синтетике врёт про живой голос.
   Офлайн-рендер на вокале делается одной командой, файл у пользователя есть:

   ```
   ./build/ProcessorTest_artefacts/Release/ProcessorTest \
       --render out.wav hq "$HOME/Downloads/audio [vocals].wav" colour
   ```

   `colour` включает диффузию 60 %, фильтры на значениях по умолчанию и feedback 45 %.
   Без него окраска петли выключена — это A/B к тому же файлу.

## Готово, когда
- [ ] #46: при глубине 0 выход бит-в-бит прежний, на feedback 90 % металлика слабее,
      модуляция не читается как расстройка, решение по развилке записано с обоснованием
- [ ] #47: при глубине 0 выход бит-в-бит прежний, хвост проявляется в паузах,
      накачки на плотном материале нет, одинаково работает в обоих режимах Time Mode
- [ ] #23: корреляция в моно замерена, фазового вычитания нет; про ping-pong принято
      решение — сделан или отвергнут с причиной
- [ ] Новые параметры выведены в окно плагина
- [ ] Проверено в обоих режимах Time Mode
- [ ] Проверено на живом вокале, а не только на синтетике
- [ ] `cmake --build build --target ProcessorTest && ./build/ProcessorTest_artefacts/Release/ProcessorTest` — зелёный
- [ ] Офлайн-тесты буфера и питчера собираются одной командой clang и зелёные
      (команды — в шапке `Source/DSP/test_delay_buffer.cpp` и `test_pitch_shifter.cpp`)
- [ ] pluginval strictness 10 зелёный, `auval -v aumf Mdly Mkev` зелёный
- [ ] Изменения закоммичены, сообщение на русском
- [ ] `prompts/PROGRESS.md` обновлён
- [ ] Этот файл перемещён в `prompts/archive/S13-modulation.md`
- [ ] Написан новый `prompts/NEXT.md` для сессии 14
- [ ] Написано стартовое сообщение для сессии 14 в чат

## Следующая сессия займётся
Закрытием вехи M3 (#20 tempo sync, #21 анти-клик, если что-то из него ещё осталось)
и подготовкой к M4. Первым делом M4 — это **#48, четыре громких пресета и получасовой
ручной тест в FL**: `docs/STRATEGY.md` §9 требует их до того, как сверху ляжет ещё код.

## Известные риски

1. **Три узла в одной петле складываются нелинейно.** Диффузия и фильтры уже стоят
   в петле, модуляция встанет третьей. Проверять усиление петли на feedback 95 %
   надо со всеми тремя сразу, а не по одному: в сессии 12 это было записано как риск
   и оказалось верным — алл-пасс не растит уровень, но удлиняет затухание с 0,95
   до 0,97 за круг.
2. **`getTailLengthSeconds` уже занижает хвост примерно в полтора раза** при плотной
   диффузии: оценка считает `feedback^n`, а реальный круг петли на части частот длиннее
   из-за фазы алл-пасса. Модуляция времени добавит к этому ещё. Пока прячется за потолком
   в 20 с; если после #46 хвост станет ещё длиннее — это повод посмотреть, а не молчать.
3. **Слуховая часть #19 закрыта измерением, а не ухом.** Агент не слушает. Если
   пользователь послушает `~/Downloads/S12-renders/` и скажет, что согласные звучат
   плохо, — это разговор про ADR 0005 **до** модуляции и ducking, а не после.
   Числа за то, что всё в порядке: корреляция огибающих 0,95–0,98, крутизна атаки
   72–99 % от сухого.
4. **Рабочий стол прибран в сессии 12** — рендеры отладки, записи экрана и скриншоты
   удалены по явному согласию пользователя. Материал был отработан, числа из него
   лежат в #19 и ADR 0006. Заново не искать.
